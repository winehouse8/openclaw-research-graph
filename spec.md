# spec.md

## 프로젝트명
**openclaw-research-graph**

## 목적
OpenClaw를 위한 **컨티뉴얼 리서치 시스템**을 구축한다.

이 시스템은 OpenClaw가:
- 특정 주제에 대해 반복적으로 리서치를 수행하고
- 시간이 지나면서 새로 추가되는 외부 정보를 계속 반영하고
- 기존 답을 갱신하고
- 축적된 지식을 재사용할 수 있도록 해야 한다.

궁극적으로는 OpenClaw가 매일 아침 9시처럼 정해진 시간에 자동으로 특정 주제를 다시 조사하고,
새 정보가 생기면 기존 답을 더 나은 방향으로 업데이트할 수 있어야 한다.

이 프로젝트는 단발성 요약기가 아니라,
장기적으로 축적되고 재사용되는 **리서치 메모리 및 retrieval 시스템**이다.

---

## 핵심 목표
이 시스템은 다음과 같은 질문을 장기적으로 계속 조사할 수 있어야 한다.

예:
- 16GB 맥미니에서 가장 성능이 좋은 로컬 LLM은 무엇인가?
- OpenClaw의 메모리 기능 향상을 위해 가장 좋은 agentic memory 서비스는 무엇인가?

즉 이 프로젝트의 본질은,
**새 정보가 계속 생기는 질문에 대해 OpenClaw가 장기적으로 답을 관리하고 개선하는 것**이다.

---

## 필수 요구사항
이 시스템은 최소한 다음을 지원해야 한다.

### 1. 지식 축적
- 리서치 결과를 구조화해서 저장할 수 있어야 한다
- 이후 리서치에서 그 저장물을 다시 재사용할 수 있어야 한다

### 2. 효율적인 지식 관리
지식 그래프 또는 이에 준하는 저장 계층은 다음을 지원해야 한다.
- CRUQD
  - Create
  - Read
  - Update
  - Query
  - Delete
- 중복 감지
- RAG 가능성

즉 이 시스템은 단순 저장소가 아니라,
**효율적인 지식 관리 엔진**이어야 한다.

### 3. 컨티뉴얼 리서치
- cold-start 상황에서는 외부 검색만으로 시작할 수 있어야 한다
- 이후에는 축적된 메모리와 새 외부 정보를 함께 사용할 수 있어야 한다
- 새 정보가 들어오면 기존 결론을 수정할 수 있어야 한다

### 4. OpenClaw 통합
OpenClaw가 이 시스템을 직접 사용할 수 있어야 한다.
OpenClaw는 이를 통해:
- research objective를 만들거나 선택하고
- 관련 지식을 읽고
- 새 리서치를 수행하고
- 결과를 축적하고
- 이후 reasoning에서 다시 재사용할 수 있어야 한다

---

## 핵심 원칙
- 설계는 단순하게 유지할 것
- 과설계 금지
- ambiguity가 있으면 보수적으로 처리할 것
- 애매한 objective/source 자동 병합 금지
- retrieval purity를 중요하게 볼 것
- source와 thinking은 뒤섞이지 않도록 관리할 것
- OpenClaw가 장기적으로 반복 실행해도 구조가 망가지지 않아야 할 것

---

## 성공 기준
다음을 만족하면 성공이다.

- OpenClaw가 특정 주제를 장기적으로 반복 리서치할 수 있다
- 축적된 지식을 이후 리서치에서 다시 사용할 수 있다
- 새로운 외부 정보가 들어오면 기존 답을 갱신할 수 있다
- 지식 저장 계층이 효율적인 CRUQD / dedupe / RAG를 지원한다
- cold-start와 memory-augmented mode가 모두 동작한다
- 매일 오전 9시 자동 리서치로 확장 가능한 구조가 마련된다

---

## 구현 힌트 (비구속)
아래 내용은 강제 명세가 아니라 구현 힌트다.
필요하면 더 나은 구조를 선택해도 된다.

### 저장 계층
- Neo4j 기반 지식 그래프를 우선 고려한다
- topic, research objective, source, thinking 같은 계층이 유용할 가능성이 높다

### retrieval 방향
- source retrieval과 thinking retrieval은 분리하는 편이 유리할 수 있다
- retrieval 흐름은 대체로 **필터링 → RAG → 관련도 판단** 구조가 적합할 가능성이 높다
- cold-start에서는 외부 검색 우선, 이후에는 memory-augmented retrieval로 전환하는 방식이 유효할 수 있다

### reasoning 방향
- actor / critic 구조가 품질 향상에 도움될 수 있다
- retrieval 계층과 reasoning 계층은 분리하는 편이 바람직하다

### 운영 방향
- 이 시스템은 OpenClaw가 장기적으로 사용할 research skill이 되어야 한다
- 장기적으로는 매일 오전 9시 자동 리서치를 수행하는 방향까지 고려한다

---

## spec.md 사용 원칙
이 문서는 구현 세부를 과도하게 고정하기 위한 문서가 아니다.
이 문서는 프로젝트의 목적, 성공 기준, 핵심 원칙을 유지하기 위한 북극성이다.

즉:
- 위쪽은 강한 목적과 원칙
- 아래쪽 힌트는 참고용

구현 세부는 실험과 검증을 통해 더 나은 방향으로 발전할 수 있다.
